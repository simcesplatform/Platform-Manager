# Example for defining the GitHub repositories where manifest files should be fetched

Repositories:
    - simcesplatform/simulation-manager:
        File: component_manifest_manager.yml
    - simcesplatform/simulation-manager:
        File: component_manifest_dummy.yml
    - simcesplatform/logwriter

Type: GitHub

# only needed for private repositories, uncomment if necessary
# AccessToken: ${GITHUB_ACCESS_TOKEN}
